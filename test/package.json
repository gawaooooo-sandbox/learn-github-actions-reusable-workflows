{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "format:check": "npx prettier --parser=\"yaml\" --check --no-error-on-unmatched-pattern \"**/*.y*ml\"",
    "format:fix": "npx prettier --parser=\"yaml\" --write --no-error-on-unmatched-pattern \"**/*.y*ml\"",
    "test": "echo \"call test (test directory) \"",
    "test:script": "npm run format:check",
    "lint": "echo \"call lint  (test directory) \"",
    "build": "echo \"call build  (test directory) \" && node index.js",
    "generate": "echo \"call generate  (test directory) \""
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "gsap": "^3.12.5"
  }
}

{
  "name": "learn-github-actions-reusable-workflows",
  "version": "1.0.0",
  "description": "GitHub Actions reusable workflows",
  "main": "index.js",
  "scripts": {
    "prepare": "husky",
    "format:check": "npx prettier --parser=\"yaml\" --check \"**/*.y*ml\"",
    "format:fix": "npx prettier --parser=\"yaml\" --write \"**/*.y*ml\"",
    "test": "echo \"call test \"",
    "test:script": "npm run format:check",
    "lint": "echo \"call lint \"",
    "build": "echo \"call build \"",
    "generate": "echo \"call generate \""
  },
  "lint-staged": {
    "*.{yml,yaml}": [
      "npx prettier --parser=yaml --check"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^15.2.2"
  }
}
